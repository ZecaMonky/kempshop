<%- contentFor('body') %>
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body text-center">
                    <div class="mb-4">
                        <i class="fas fa-check-circle text-success" style="font-size: 64px;"></i>
                    </div>
                    <h1 class="mb-4">Заказ успешно оформлен!</h1>
                    <p class="lead mb-4">Номер вашего заказа: #<%= order.id %></p>
                    <p class="mb-4">Мы отправили подтверждение на ваш email: <%= user.email %></p>
                    
                    <div class="card mb-4">
                        <div class="card-body">
                            <h5 class="card-title mb-3">Детали заказа</h5>
                            <div class="row">
                                <div class="col-sm-6 text-start">
                                    <p><strong>Статус:</strong> <%= order.status %></p>
                                    <p><strong>Способ доставки:</strong> <%= order.delivery_method %></p>
                                    <p><strong>Способ оплаты:</strong> <%= order.payment_method %></p>
                                </div>
                                <div class="col-sm-6 text-start">
                                    <p><strong>Дата заказа:</strong> <%= new Date(order.created_at).toLocaleDateString() %></p>
                                    <p><strong>Сумма заказа:</strong> <%= order.total_price %> ₽</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex justify-content-center gap-3">
                        <a href="/" class="btn btn-primary">На главную</a>
                        <a href="/profile/orders" class="btn btn-outline-primary">Мои заказы</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 